{% extends 'index.twig' %}
{% block content %}
<div class="centerdiv">
<div class="card">
    <h1>Categorie</h1>
    <p>ID: {{id}}</p>
    <p>Libelle: {{libelle}}</p>
    <p>Description: {{description}}</p>
    <a class="bouton" href="{{ url_for('prestationsCategorie', {'id':id}) }}">Voir les prestations de la catégorie</a>
</div>
</div>
    <br>
    <div class="centerdiv">
<a class="bouton" href="{{ url_for('categories') }}">Retour à la liste des catégories</a>
</div>
{% endblock %}