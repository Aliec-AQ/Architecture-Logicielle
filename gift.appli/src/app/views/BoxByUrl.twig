{% extends 'index.twig' %}
{% block content %}
<div class="box-container">
    <section class="box-info">
        <div class="displayCard">
            <h2 class="info-title">{{libelle}}</h2>
            <div>
                <h3>Description de la box : </h3>
                <p class="info-description">{{ description }}</p>
            </div>
            {% if  kdo %}
            <div class="gift">
                <h2 class="gift-title">- Cadeau -</h2>
                <p class="gift-message">{{ message_kdo }}</p>
            </div>
            {% endif %}
        </div>
    </section>
    <section class="box-prestations">
        <table class="prestations-table">
            <thead>
                <tr class="prestation-row">
                    <th>Prestation</th>
                    <th>Quantité</th>
                    {% if not kdo %}
                    <th>Tarif unitaire</th>
                    <th>Prix</th>
                    {% endif %}
                </tr>
            </thead>
            <tbody>
                {% for prestation in prestations %}
                <tr class="prestation-row">
                    <td>{{ prestation.libelle }}</td>
                    <td>{{ prestation.pivot.quantite }}</td>
                    {% if not kdo %}
                    <td>{{ prestation.tarif }} €</td>
                    <td>{{ prestation.tarif * prestation.pivot.quantite  }} €</td>
                    {% endif %}
                </tr>
                {% endfor %}
            </tbody>
            {% if not kdo %}
            <tfoot>
                <tr class="prestation-row">
                    <td><h3>Montant total</h3></td>
                    <td></td>
                    <td></td>
                    <td>{{ montant }} €</td>
                </tr>
            </tfoot>
            {% endif %}
        </table>
    </section>
</div>
{% endblock %}