{% extends 'index.twig' %}
{% block content %}
<h1>Box créée</h1>
<div class="card">
        <h2>Information de la Box</h2>
            <p>Nom: {{ postData.name }}</p>
            <p>Description: {{ postData.   description }}</p>
    </div>
    </div>
<br>
<a class="bouton" href="{{ url_for('root') }}">Retour au menu</a>

{% endblock %}